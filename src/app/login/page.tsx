import AuthRouteBackdrop from "@/components/auth/AuthRouteBackdrop";

export const metadata = {
  title: "Login — Trading Santai",
};

export default function LoginPage() {
  return <AuthRouteBackdrop />;
}