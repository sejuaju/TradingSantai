/** Latar belakang minimal saat /login atau /signup — modal ditangani AuthModalProvider */
export default function AuthRouteBackdrop() {
  return <div className="min-h-screen bg-[#05060c]" aria-hidden />;
}